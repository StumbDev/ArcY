const { isJsxFragment } = require("typescript");

document.getElementById("CommandBarCMD");
document.getElementById("enterBtn");

if (CommanBarCMD == "browse") {
  window.location.href = "";
  // put the mini Browser ejs file location
}

function Minibrowser() {
  document.getElementById(urlBar);
  document.getElementById(goBtn);
  if (urlBar === null) {
    // do nothing
  }
  if (urlBar === "google") {
    // handle this later
  }
}
